<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.portflux.backend.mapper.CommentMapper">

    <select id="findCommentsByPostId" parameterType="int" resultType="com.portflux.backend.beans.CommentDto">
        SELECT
            c.comment_id AS commentId,
            c.user_num AS userNum,
            u.user_nickname AS userNickname,
            u.user_image AS userImage,
            c.comment_content AS commentContent,
            c.comment_created_at AS commentCreatedAt
        FROM
            POST_COMMENT c
        JOIN
            USERS u ON c.user_num = u.user_num
        WHERE
            c.post_id = #{postId}
        ORDER BY
            c.comment_created_at ASC
    </select>

    <insert id="addComment" parameterType="com.portflux.backend.beans.CommentDto">
        <selectKey keyProperty="commentId" resultType="int" order="BEFORE">
            SELECT NVL(MAX(comment_id), 0) + 1 FROM POST_COMMENT
        </selectKey>
        INSERT INTO POST_COMMENT (
            comment_id,
            post_id,
            user_num,
            comment_content,
            comment_created_at
        ) VALUES (
            #{commentId},
            #{postId},
            #{userNum},
            #{commentContent},
            SYSDATE
        )
    </insert>

    <select id="findCommentById" parameterType="int" resultType="com.portflux.backend.beans.CommentDto">
        SELECT
            comment_id AS commentId,
            user_num AS userNum,
            comment_content AS commentContent,
            comment_created_at AS commentCreatedAt
        FROM
            POST_COMMENT
        WHERE
            comment_id = #{commentId}
    </select>

    <delete id="deleteComment" parameterType="int">
        DELETE FROM POST_COMMENT WHERE comment_id = #{commentId}
    </delete>

</mapper>
